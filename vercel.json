{
    "version": 2,
    "name": "quasar-app",
    "scripts": {
        "build": "quasar build",
        "deploy": "vercel"
    },
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/.*",
            "dest": "/index.html"
        }
    ]
}