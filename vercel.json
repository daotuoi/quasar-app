{
    "scripts": {
        "build": "quasar build",
        "deploy": "vercel"
    },
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/.*",
            "dest": "/index.html"
        }
    ]
}